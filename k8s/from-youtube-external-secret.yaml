apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: from-youtube-secrets
  namespace: mirrorfm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: from-youtube-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: homeplane/from-youtube
