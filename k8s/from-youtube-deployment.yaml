apiVersion: apps/v1
kind: Deployment
metadata:
  name: from-youtube
  namespace: mirrorfm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: from-youtube
  template:
    metadata:
      labels:
        app: from-youtube
    spec:
      containers:
        - name: from-youtube
          image: mirrorfm-from-youtube:latest
          imagePullPolicy: Never
          env:
            - name: MIN_INTERVAL
              value: "1"
            - name: SHORT_IDLE
              value: "5"
          envFrom:
            - secretRef:
                name: from-youtube-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
